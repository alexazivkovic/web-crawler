# Konkurentno Programiranje Template

Minimalni C template za studente.

## Quick start

```bash
./build
./build --run
./build --clean
./build --release
```

## Struktura

- `src/` - izvorni kod
- `include/` - header fajlovi
- `out/` - build output
